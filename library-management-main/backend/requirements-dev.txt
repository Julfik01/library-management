# backend/requirements-dev.txt
# Development and testing dependencies only — not installed in production image

-r requirements.txt

# Testing
pytest==8.3.4
pytest-asyncio==0.23.8
httpx==0.27.2
factory-boy==3.3.3
aiosqlite==0.20.0  # Required by test_integration_smoke.py (SQLite in-memory tests)
