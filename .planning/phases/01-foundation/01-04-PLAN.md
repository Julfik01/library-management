---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - frontend/src/pages/CreateLibrarianPage.tsx
  - frontend/src/pages/UnauthorizedPage.tsx
  - frontend/src/App.tsx
  - frontend/src/components/AdminNavLink.tsx
autonomous: false
requirements: [AUTH-06, AUTH-07]
tags: [react, shadcn, rbac, admin]

must_haves:
  truths:
    - "An admin librarian can open /admin/users/new and create a librarian account, seeing a success toast"
    - "A non-admin who navigates to /admin/users/new is redirected to /unauthorized (client guard)"
    - "A student calling POST /admin/users directly still receives 403 from the backend (AUTH-07)"
    - "The seeded admin can log in and create a librarian who can then log in (full AUTH-06 loop)"
  artifacts:
    - path: "frontend/src/pages/CreateLibrarianPage.tsx"
      provides: "Create Librarian screen per UI-SPEC Screen 4 (AUTH-06)"
      contains: "Create librarian account"
    - path: "frontend/src/pages/UnauthorizedPage.tsx"
      provides: "Access-denied screen per UI-SPEC Screen 5 (AUTH-07)"
      contains: "Access denied"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "ProtectedRoute allowedRoles={['admin_librarian']}"
      via: "/admin/users/new route is wrapped so non-admins hit /unauthorized"
      pattern: "allowedRoles"
    - from: "frontend/src/pages/CreateLibrarianPage.tsx"
      to: "POST /admin/users"
      via: "react-query mutation via the api axios instance"
      pattern: "admin/users"
---

<objective>
Close the phase with the admin-only vertical slice and the RBAC verification: the Create Librarian screen (AUTH-06) gated to admin_librarian, the Unauthorized screen (AUTH-07), the admin nav link, and the route wiring that sends non-admins to /unauthorized. Then verify end-to-end that the seeded admin can create a librarian who can subsequently log in, and that backend RBAC blocks a student even when the frontend guard is bypassed.

Purpose: Completes AUTH-06 and the user-visible side of AUTH-07, and proves the defense-in-depth model from D-09/CM-7 — the client guard is UX, the backend require_role is the authority. This is the final MVP slice for the Foundation user story.
Output: Create Librarian + Unauthorized screens, admin nav link, route guarding, and full-flow verification (UI + curl-bypass).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Librarian + Unauthorized screens, admin nav link, and route guarding</name>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md (Screen 4 Create Librarian, Screen 5 Unauthorized; Copywriting Contract; Route Map; Component Inventory: sonner toast; Accessibility Contract)
    - .planning/phases/01-foundation/01-03-SUMMARY.md (existing routing structure, ProtectedRoute usage, axios api instance, AuthContext shape)
    - .planning/phases/01-foundation/01-02-SUMMARY.md (POST /admin/users contract: request body + 409/403 responses)
  </read_first>
  <files>frontend/src/pages/CreateLibrarianPage.tsx, frontend/src/pages/UnauthorizedPage.tsx, frontend/src/components/AdminNavLink.tsx, frontend/src/App.tsx</files>
  <action>
    Build `CreateLibrarianPage.tsx` per UI-SPEC Screen 4: a centered Card with Form (react-hook-form + zod: full_name, email valid, password) and the exact copy ("Create librarian account", subhead "The new librarian will be able to log in immediately.", CTA "Create account"/"Creating account..."). Submit via a TanStack Query mutation to POST /admin/users using the `api` axios instance; on success show the Sonner toast "Librarian account created.", reset the form, and keep the user on the page (per Screen 4 success behavior); on 409 show Alert "An account with this email already exists."; on 403 show "You don't have permission to perform this action." Include the "← Back to dashboard" link. Build `UnauthorizedPage.tsx` per Screen 5: centered, no nav, ShieldOff lucide icon, heading "Access denied", body "You don't have permission to view this page.", and a "Go to dashboard" outline Button → navigate('/dashboard'). Create `AdminNavLink.tsx` rendering the "Manage Users" nav link only when the current user role is admin_librarian (linking to /admin/users/new); integrate it into the Dashboard TopNav from plan 03 (admin row of the nav-links-by-role table). In `App.tsx`, add the `/admin/users/new` route wrapped in `ProtectedRoute allowedRoles={['admin_librarian']}` and the `/unauthorized` route → UnauthorizedPage (replace the plan-03 placeholder). Colors via CSS variables only; follow the Accessibility Contract.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -5 && grep -q "Create librarian account" src/pages/CreateLibrarianPage.tsx && grep -q "Access denied" src/pages/UnauthorizedPage.tsx && grep -q "allowedRoles" src/App.tsx && echo "admin ui ok"</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` succeeds with no TypeScript errors
    - Create Librarian form posts to /admin/users and shows the success toast on 201, the 409 alert, and the 403 alert with the exact UI-SPEC copy
    - /admin/users/new is wrapped in ProtectedRoute allowedRoles=['admin_librarian']
    - /unauthorized renders the Access denied screen with the ShieldOff icon and "Go to dashboard"
    - "Manage Users" nav link shows only for admin_librarian
  </acceptance_criteria>
  <done>Admin screens build and are correctly route-guarded; copy matches the UI-SPEC.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The complete Foundation phase end-to-end: seeded admin login, admin creates a librarian, the new librarian logs in, plus a backend-RBAC bypass check proving the client guard is not the only defense.</what-built>
  <how-to-verify>
    1. With the stack running (`docker compose up`), visit http://localhost:5173/login and log in as the seeded admin (ADMIN_EMAIL / ADMIN_PASSWORD from your .env). Expect the "Admin Dashboard" welcome and a "Manage Users" nav link.
    2. Click "Manage Users" (or visit /admin/users/new). Create a librarian (full name, a fresh email, a password). Expect the toast "Librarian account created." and the form to reset.
    3. Sign out, then log in as that new librarian. Expect the "Librarian Dashboard" welcome (AUTH-06 full loop proven).
    4. Client guard: as the librarian (or a student), navigate directly to http://localhost:5173/admin/users/new. Expect redirect to /unauthorized showing "Access denied".
    5. Backend RBAC (curl bypass): log in as a student via the API and POST /admin/users with that student's access token, e.g.
       `curl -i -X POST http://localhost:8000/admin/users -H "Authorization: Bearer <student_access_token>" -H "Content-Type: application/json" -d '{"email":"x@e.com","password":"password123","full_name":"X"}'`
       Expect HTTP 403 (AUTH-07 — backend-enforced, not frontend-only).
    6. Optional: open MailHog at http://localhost:8025 to confirm no errors (no emails expected in Phase 1).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issue (especially if step 5 returns anything other than 403).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client route guard → admin screen | UX gate; must not be the sole control |
| client → POST /admin/users | Role-elevated mutation; backend must re-check |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Elevation of Privilege | non-admin bypassing the client guard to create librarians | mitigate | Backend require_role('admin_librarian') on POST /admin/users returns 403 regardless of client (AUTH-07, D-09, CM-7); verified by the curl-bypass checkpoint step |
| T-04-02 | Spoofing | trusting client role state for authorization | accept-with-control | ProtectedRoute/AdminNavLink are UX only; authorization decided server-side; documented |
| T-04-03 | Information Disclosure | leaking whether an email exists via create-librarian error | accept | 409 "already exists" is acceptable for an admin-only authenticated endpoint; admins are trusted operators |
| T-04-SC | Tampering | npm installs | mitigate | No new packages beyond plan 03's audited set; nothing to verify |
</threat_model>

<verification>
- `npm run build` clean
- Human checkpoint approved: admin creates librarian → librarian logs in (AUTH-06)
- curl bypass returns 403 (AUTH-07 backend-enforced)
- Non-admin → /admin/users/new redirects to /unauthorized
</verification>

<success_criteria>
- The seeded admin can create a librarian from the admin UI (AUTH-06)
- Backend RBAC blocks a student even when the client guard is bypassed (AUTH-07)
- All five Phase 1 success criteria from the ROADMAP are demonstrably TRUE
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done.
</output>
